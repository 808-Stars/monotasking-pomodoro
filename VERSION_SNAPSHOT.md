# 日报功能前版本快照

此目录是“设计一个日报/周报/月报功能”修改之前的项目副本。

## 快照范围

- 保留报告功能之前已经存在的页面、性能优化、番茄钟功能和数据库迁移。
- 移除了数据报告页面、报告路由、报告数据查询、自适应工作法分析、计时遥测以及对应迁移。
- 未修改原项目目录：`单×番-工作法_v0.2.0`。
- 此快照未复制 `node_modules`、`dist` 和本地运行日志；首次运行前在 `frontend` 目录执行 `npm install`。

## 启动

```powershell
cd frontend
npm install
npm run dev -- --port 3000
```

