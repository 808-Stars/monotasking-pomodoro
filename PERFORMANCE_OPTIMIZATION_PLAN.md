# MONOPOMO 性能问题与完整优化方案

> 适用项目：MONOPOMO（单×番-工作法）
>
> 技术栈：React + TypeScript + Vite + Supabase
>
> 目标：解决页面切换时偶发 5 秒以上加载，并建立可持续扩展的性能架构。

## 一、结论概览

目前的慢加载不是单个页面的偶发故障，而是同一类问题在多个页面重复出现：

1. 页面初始化时同时发起过多 Supabase 请求。
2. 部分统计逻辑按天循环，并且串行请求数据库。
3. 历史数据使用全量查询，没有分页、数量限制或聚合查询。
4. 多个组件重复请求同一数据，现有缓存不能合并进行中的请求。
5. 某些页面存在 N+1 查询。
6. 全局 Context 在所有受保护页面加载任务数据，扩大了每个页面的首屏开销。
7. 缺少针对查询字段、排序字段和 RLS 条件的组合索引。

建议目标：

- 首屏核心内容尽量控制在 1～3 个请求内。
- 页面加载过程中不出现串行的逐日网络请求。
- 历史列表默认分页或限制数量。
- 次要模块延迟加载，不阻塞页面主体。
- 统计数据优先使用数据库聚合或 RPC。
- 首屏主要内容在正常网络环境下 1 秒左右可见，完整内容随后补齐。

---

## 二、问题与对策

## 1. 连续打卡统计存在串行逐日请求

### 现象

`computeStreak` 和相关任务状态逻辑最多循环检查约一年的日期，每一天都单独访问 `token_records`。网络延迟会被累加，容易造成 5 秒甚至更长的等待。

### 对策 A：一次查询一年数据，前端计算

一次性读取当前用户近一年内的相关记录，再在内存中计算连续天数。

优点：改动小、容易验证、无需新增数据库函数。

缺点：数据量会随用户行为增长；适合短期修复。

### 对策 B：Supabase RPC 直接返回连续天数（推荐）

创建 `get_user_streak` 函数：

- 从数据库内部查询最近活动日期。
- 在数据库内计算当前连续天数。
- 只返回一个整数或简短对象。

优点：从最多数百次请求降为一次请求，传输量极小。

缺点：需要新增 migration，并测试时区与跨日规则。

### 对策 C：增加活动日期汇总表

增加 `user_activity_days` 表，每个用户每天最多一条记录。完成任务、番茄钟、复盘等行为时，用 upsert 写入当天活动。

查询连续打卡时只需要读取日期集合。

优点：长期性能最稳定，也便于统计月度活跃、成就和日历。

缺点：需要修改写入链路，并处理历史数据回填。

### 实施建议

先采用方案 A 或 B 消除串行请求；用户量和数据量扩大后，再升级到方案 C。

---

## 2. 工作台初始化请求过多

### 现象

Dashboard 会同时加载今日计划、任务、项目、番茄钟统计、今日会话和连续打卡。虽然部分请求使用 `Promise.all`，但总请求数仍然较多，且连续打卡内部还存在串行查询。

### 对策 A：建立工作台汇总 RPC（推荐）

使用一个 `get_dashboard_summary` RPC，一次返回：

- 今日计划。
- 今日任务数量与完成数量。
- 进行中的项目数量。
- 今日番茄钟数量与时长。
- 连续打卡天数。
- 今日奖励状态。

### 对策 B：首屏与次要内容分离

首屏只加载：

- 今日核心任务。
- 当前番茄钟状态。
- 今日完成进度。

延迟加载：

- 趋势图。
- 历史统计。
- 项目详情。
- 奖励和成就卡片。

### 对策 C：增加每日汇总数据

按用户和日期保存每日统计结果。Dashboard 首屏读取当天一条汇总记录，只有发生行为时更新该记录。

### 实施建议

短期采用 B，随后增加 A；方案 C 适合长期数据规模较大的版本。

---

## 3. 扭蛋机首屏请求过多且存在重复请求

### 现象

扭蛋机初始化会并行请求扭蛋物品、抽取记录、代币余额、每日任务、每周任务、SSR 目标、今日统计、连续打卡状态和代币流水。初始化完成后还会再次请求代币余额。

### 对策 A：拆分首屏和次要数据

首屏只加载：

- 扭蛋物品。
- 代币余额。
- 今日可抽次数或核心状态。

随后加载：

- 历史记录。
- 代币流水。
- 每日/每周任务。
- SSR 目标详情。

### 对策 B：建立扭蛋汇总 RPC

创建 `get_gacha_summary`，一次返回余额、今日次数、SSR 状态、任务进度和最近记录。

### 对策 C：保留现有接口但共享数据

- 删除初始化后的重复 `getTokenBalance`。
- Dashboard 和扭蛋机共享连续打卡结果。
- 今日代币、今日抽取次数等数据只在同一页面生命周期内请求一次。

### 额外建议

抽取历史默认只取最近 20～50 条；完整历史改为“加载更多”。

---

## 4. 缓存不能合并进行中的请求

### 现象

当前缓存只保存已经完成的数据。如果两个组件在同一时间请求相同数据，第二个请求无法复用第一个正在进行的 Promise。

### 对策

增加 `pendingCache`：

```ts
const pendingCache = new Map<string, Promise<unknown>>();
```

请求流程：

1. 优先返回有效数据缓存。
2. 如果存在相同 key 的进行中 Promise，直接复用。
3. 否则发起请求并保存 Promise。
4. 请求结束后移除 `pendingCache`。

缓存 key 必须包含用户 ID，避免不同用户之间复用错误数据。

---

## 5. 历史数据全量查询

### 涉及页面

- 扭蛋机历史记录和代币流水。
- 每日计划历史。
- 番茄钟历史。
- 复盘记录。
- 快速备忘录。

### 对策 A：分页

统一使用 `limit + range` 或基于游标的分页。列表默认加载 20～50 条，按钮或滚动加载更多。

### 对策 B：时间范围

默认只查询当前月或最近 90 天，历史月份由用户主动选择。

### 对策 C：统计与明细分离

页面顶部的数量、总时长、趋势等信息使用聚合查询；下方明细使用分页查询。

### 对策 D：按游标分页（长期推荐）

使用 `created_at` 和唯一 ID 作为游标，避免大 offset 在数据量大时变慢。

---

## 6. 复盘页面重复请求

### 现象

复盘页面可能同时执行初始加载和全量加载；在没有筛选条件时，两者查询相同数据。现有缓存也不能合并进行中的请求。

### 对策

- 统一为一个 `loadReviews` 流程。
- 筛选、排序和分页尽量在一次请求中完成。
- 使用 `pendingCache` 合并相同请求。
- 统计数量使用独立的 `count` 查询或 RPC，不读取全部复盘正文。

---

## 7. 设置页反馈评论存在 N+1 查询

### 现象

先读取反馈列表，然后每条反馈再查询评论。反馈越多，请求越多。

### 对策 A：批量读取评论

先取得反馈 ID，再使用 `feedback_id IN (...)` 一次取回评论，前端按 ID 分组。

### 对策 B：数据库视图或 RPC

使用评论数量聚合返回 `comment_count`，点击某条反馈时才加载评论明细。

### 对策 C：折叠面板懒加载（推荐）

列表只显示评论数量，展开某条反馈时才加载该条评论。

---

## 8. 每日计划页初始化数据量过大

### 现象

页面同时加载计划历史、今日计划和任务列表；计划历史可能是全量读取。

### 对策

- 计划历史按月份查询。
- 今日计划单独请求并优先渲染。
- 任务选择器只加载进行中的任务，或使用搜索式分页。
- 日历切换月份时才请求对应月份数据。

---

## 9. 番茄钟历史页查询过重

### 现象

页面同时读取全部会话，并执行多个统计 count 查询。

### 对策 A：列表分页 + 统计聚合

列表只读取当前页；总次数、总时长、完成率由一个聚合 RPC 返回。

### 对策 B：按日期范围查询

默认显示最近 30 天，用户选择日期范围后再查询。

### 对策 C：每日统计表

按用户和日期保存番茄钟次数、完成分钟数、完成率，历史图表直接读取汇总数据。

---

## 10. 任务页和项目页数据未充分限制

### 现象

任务列表可能全量读取；项目详情和项目统计可能重复读取任务。

### 对策

- 任务列表增加状态、项目、日期筛选和分页。
- 项目卡片只显示任务数量等聚合字段。
- 点击项目后才读取任务明细。
- 项目统计使用 `tasks(count)` 或专用聚合 RPC，避免反复读取完整任务对象。

---

## 11. 全局 Pomodoro Context 放大所有页面开销

### 现象

受保护布局加载时，Context 同时读取全部任务和进行中任务。即使用户当前不在番茄钟页面，也会承担这部分请求。

### 对策 A：只读取进行中的任务

如果全局只需要恢复当前计时，直接请求 `status = IN_PROGRESS`。

### 对策 B：仅在番茄钟页面加载

将任务选择和相关数据移动到 Pomodoro 页面。

### 对策 C：全局只保留计时状态

Context 只保存 localStorage 中的 timer 状态，任务详情进入番茄钟页面后再读取。

---

## 12. Showcase 页面每次组合读取原始数据

### 现象

展示页需要余额、番茄钟总数、扭蛋记录和物品信息。随着历史数据增长，原始数据组合查询会逐渐变慢。

### 对策

- 使用 `get_showcase_summary` RPC 直接返回展示所需指标。
- 月度快照只查询指定月份。
- 物品定义使用长期缓存。
- 如果展示指标固定，增加用户月度汇总表。

---

## 13. 数据库缺少组合索引

应根据真实查询计划确认后增加索引，建议重点检查：

```sql
create index if not exists idx_token_records_user_created
  on public.token_records (user_id, created_at desc);

create index if not exists idx_token_records_user_source_created
  on public.token_records (user_id, source, created_at desc);

create index if not exists idx_gacha_records_user_created
  on public.gacha_records (user_id, created_at desc);

create index if not exists idx_pomodoro_sessions_user_start
  on public.pomodoro_sessions (user_id, start_time desc);

create index if not exists idx_reviews_user_date
  on public.reviews (user_id, date desc);

create index if not exists idx_daily_plans_user_date
  on public.daily_plans (user_id, date desc);

create index if not exists idx_tasks_user_status_created
  on public.tasks (user_id, status, created_at desc);
```

索引上线前应结合 Supabase 的 `EXPLAIN ANALYZE` 检查，避免盲目增加索引导致写入变慢。

---

## 14. RLS 与 RPC 安全边界需要同步审查

性能优化不能牺牲数据安全。特别是 `SECURITY DEFINER` RPC 必须：

- 使用 `auth.uid()` 获取当前用户。
- 不信任前端传入的 `user_id`、日期和月份。
- 校验抽取次数只能是允许的值，例如 1 或 10。
- 设置安全的 `search_path`。
- 明确执行者和函数权限。

另外，不能允许客户端通过直接插入 `token_records` 自行制造奖励。奖励写入应移动到受控 RPC 或 Edge Function 中，并由服务端校验行为是否真实完成。

---

## 15. 缓存策略应按数据类型区分

建议缓存时间：

| 数据类型 | 建议策略 |
|---|---|
| 扭蛋物品定义 | 长缓存，版本变化时失效 |
| 用户设置 | 数分钟缓存，修改后主动失效 |
| 今日计划和任务 | 短缓存，写入后主动失效 |
| 余额与奖励状态 | 不使用长缓存，操作后立即刷新 |
| 历史列表 | 分页缓存，按页或游标失效 |
| Dashboard 汇总 | 短缓存或行为写入时更新 |

可采用 stale-while-revalidate：先展示最近缓存，同时后台刷新最新数据。

---

## 16. 页面结构应采用渐进式渲染

统一页面加载状态：

1. 立即渲染页面骨架和导航。
2. 优先显示核心数字、主要按钮和当前任务。
3. 次要卡片使用独立 skeleton。
4. 图表、历史、评论和扩展信息后台加载。
5. 单个次要请求失败时，不阻塞整页。

不要让一个“历史统计”请求决定整个页面是否显示。

---

## 17. 增加性能观测，避免凭感觉优化

为每个页面记录：

- 首次渲染时间。
- 核心数据可见时间。
- 完整页面加载时间。
- 每个 Supabase 请求耗时。
- 请求失败率和超时次数。

可使用 `performance.mark` / `performance.measure`，仅在开发环境或抽样用户中输出日志。重点关注超过 1 秒的请求。

---

## 三、推荐实施顺序

### 第一阶段：低风险快速修复

1. 增加进行中 Promise 缓存。
2. 删除扭蛋机重复余额请求。
3. 合并复盘页面重复加载。
4. 为所有历史列表增加默认 limit 和分页。
5. 设置页评论改为展开时加载。
6. Dashboard、扭蛋机采用核心内容优先渲染。
7. 增加请求耗时日志。

### 第二阶段：消除主要慢点

1. 将连续打卡改为单次查询或 RPC。
2. Dashboard 与扭蛋机共享连续打卡和今日统计结果。
3. 用聚合查询替代多个 count 请求。
4. 将全局 Pomodoro Context 改为只加载必要状态。

### 第三阶段：数据库级优化

1. 增加并验证组合索引。
2. 创建 `get_dashboard_summary`。
3. 创建 `get_gacha_summary`。
4. 创建番茄钟、复盘、展示页的统计 RPC。
5. 同步修复 RPC 的 `auth.uid()` 与参数校验。

### 第四阶段：长期架构优化

1. 建立 `user_activity_days`。
2. 建立每日/月度统计表。
3. 将报表和展示页改为读取汇总数据。
4. 建立性能基线和回归检查。

---

## 四、理想请求结构

| 页面 | 理想首屏请求数 | 次要数据策略 |
|---|---:|---|
| 工作台 | 1～2 | 图表与历史延迟加载 |
| 扭蛋机 | 1～2 | 流水和完整历史分页 |
| 每日计划 | 1～2 | 按月、按需加载 |
| 番茄钟历史 | 1～2 | 列表分页，统计聚合 |
| 复盘 | 1 | 分页和筛选一次完成 |
| 设置反馈 | 1 | 评论展开时加载 |
| 任务 | 1 | 分页、筛选、排序服务端完成 |
| 项目 | 1 | 详情和任务延迟加载 |
| 展示页 | 1 | 读取汇总数据 |

最终应达到：没有逐日串行网络循环，没有无上限历史查询，没有页面级 N+1，请求失败不会阻塞整页，且所有用户数据查询都受 RLS 和服务端身份校验保护。

## 五、验收标准

优化完成后建议逐项验证：

- 页面切换时导航和骨架立即出现。
- Dashboard 和扭蛋机核心内容在 1 秒左右可见。
- 连续打卡查询最多产生一次数据库请求。
- 历史列表首次最多返回 20～50 条。
- 反馈列表不会为每条记录单独请求评论。
- 同一数据的并发请求只产生一次网络请求。
- 网络较慢或某个次要请求失败时，页面仍可使用。
- 用户只能读取和修改自己的数据。
- 扭蛋抽取、代币奖励和日期计算不能由客户端伪造。
- 生产构建、数据库 migration 和主要页面回归测试全部通过。

## 六、本地执行状态（2026-08-26）

已完成：

- 进行中请求合并、用户缓存隔离和认证切换清理。
- Dashboard/Gacha 核心数据优先加载。
- 连续打卡、Dashboard、Gacha、番茄钟和藏品室统计 RPC。
- 扭蛋抽取与代币奖励服务端校验边界。
- 历史读取上限、每日计划按月读取、Reviews 重复请求清理、Settings 评论懒加载。
- 项目归档页 N+1 查询清理和组合索引 migration。

当前保留项：

- 历史明细已设置默认上限，但尚未实现完整的游标式“加载更多”。
- 生产 bundle 仍有 Vite 单包超过 500KB 的提示，属于后续代码分割优化项。
- Supabase migration 的执行计划、高并发竞争和线上 RLS 仍需在实际项目中验证。
